/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: '/elnwebsitenext',
    trailingSlash: true,
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
